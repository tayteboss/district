import styled from "styled-components";

const TalentListWrapper = styled.section``;

const TalentList = () => {
  return <TalentListWrapper>TalentList</TalentListWrapper>;
};

export default TalentList;
