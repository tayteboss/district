import styled from "styled-components";

const ServicesListWrapper = styled.section``;

const ServicesList = () => {
  return <ServicesListWrapper>ServicesList</ServicesListWrapper>;
};

export default ServicesList;
